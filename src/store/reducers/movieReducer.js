const initialState = {
  test: "dizz"
};

export default function movieReducer(state = initialState, action) {
  switch (action.type) {
    // case 'ACTION_TYPE':
    //   return code
    default:
      return state;
  }
}
